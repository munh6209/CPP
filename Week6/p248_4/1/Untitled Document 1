#include <stdio.h>
#define MAX_ROWS 40
#define MAX_COLS 80

static int MineMapMask[MAX_ROWS][MAX_COLS];
static int MineMapLabel[MAX_ROWS][MAX_COLS];

int main(void) {
    int rows, cols;
    printf("가로 크기 입력 (1~80): ");
    scanf("%d", &cols);
    printf("세로 크기 입력 (1~40): ");
    scanf("%d", &rows);
    printf("맵 크기: %d x %d\n", rows, cols);
    return 0;
}
